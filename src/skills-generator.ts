import type { GravityRegistry } from './rules/registry.js';

export function generateSkillsMarkdown(_registry: GravityRegistry): string {
  return '# vue-gravity-engine\n\nPlaceholder — will be implemented in Task 13.';
}
